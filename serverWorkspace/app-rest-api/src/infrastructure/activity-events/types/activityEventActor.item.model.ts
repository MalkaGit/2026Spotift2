export type ActivityEventActorItem = {
  event_id: string;         // the id of the event
  actor_type: string;       // the type of the actor
  actor_id: string;         // the id of the actor
  actor_name: string;       // the name of the actor
};
