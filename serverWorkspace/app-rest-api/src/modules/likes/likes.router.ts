
/**
 * the likes contoller handles the endpoins below.
 * these endoints are handled different routers by thier path
 * - POST /users/me/likes'   - handled by users router       calling likes controller
 * - GET /artist/:id/likes   - handled by artists router     calling likes controller
 * - GET /album/:id/likes    - handled by albums router       calling likes controller
 * - GET /playlist/:id/likes - handled by playlists router calling likes controller
 */


