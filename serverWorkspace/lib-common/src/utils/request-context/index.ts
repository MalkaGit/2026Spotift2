/**
 * FOLDER MODULE:FOLDER API
 * ===========================================
 * This is the folder entry point.
 * It allows imports from the folder path instead of the specific file.
 * 
 * Example: import { withRequestContext, requestContext } from "./request-context";
 *          instead of: import { withRequestContext, requestContext } from "./request-context/request-context.util";
 */

export { withRequestContext, requestContext } from "./request-context.util";

